#' Oracle Driver for RJDBC
#' 
#' This is the oracle driver that is used by all `GCLr` functions that use the `RJDBC` package to pull data rom Loki.
#' 
#' @details This object was created from the ojdbc8.jar file and saved as a data object in the `GCLr` package.
"drv"